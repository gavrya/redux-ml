sudo: false

notifications:
  email: false

language: node_js

node_js:
  - node

install:
  - yarn install
  - yarn audit
  - yarn outdated

script:
  - yarn lint
  - yarn prettier
  - yarn test:coveralls
  - yarn build
