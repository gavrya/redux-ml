sudo: false

notifications:
  email: false

language: node_js

node_js:
  - node

install:
  - yarn install

script:
  - yarn audit
  - yarn outdated
  - yarn lint
  - yarn prettier
  - yarn test:coveralls
  - yarn build
